// empty component
import React from 'react';

interface ColDef {
  headerName: string;
  field: string;
  width: number;
  pinnedIndex?: number;
  filterInHeader?: boolean;
}
export default ({}) => {


}